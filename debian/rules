#!/usr/bin/make -f
%:
	dh $@

override_dh_auto_clean:
	@echo "Skipping auto_clean for setup.py (using manual layout)"

override_dh_auto_build:
	@echo "Skipping auto_build for setup.py"

override_dh_auto_install:
	@echo "Skipping auto_install for setup.py"

override_dh_fixperms:
	dh_fixperms
	chmod +x debian/imcurvin/usr/bin/imcurvin